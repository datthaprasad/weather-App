<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <div class="main">

            {{>header}}

                <form action="/weather" method="GET">
                    <img src="/img/weather.jpeg"/>
                    <div class="formSearch">

                        <input type="search" placeholder="Search" name="address" id="search"/>
                        <input type="submit" value="Get weather report" id="submit"/>

                    </div>
                </form>

                <div class="result">
                    <h1>Your Data will appear here.</h1>
                    <h4>{{error}}</h4>
                    <h3>{{location}}</h3>
                    <h2>{{forecastData}}</h1>
                </div>
            

        </div>

        {{>footer}}
        <script src="/js/app.js"></script>
        
    </body>
</html>
